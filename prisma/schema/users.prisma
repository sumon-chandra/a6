model User {
    id        String   @id @default(uuid())
    name      String
    email     String   @unique
    password  String
    role      String
    avatar    String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("users")
}
